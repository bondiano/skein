// M5: одна строка — Skein-плагин поверх Loom берёт на себя clojure source set,
// AOT, reflection enforcement, mappings embedding и nREPL wiring:
//
// plugins {
//     id("dev.skein.fabric-clojure") version "<version>"
// }
