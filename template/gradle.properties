# Your mod's Maven coordinates and version. `version` is substituted into
# fabric.mod.json at build time (see build.gradle.kts).
group=com.example
version=0.1.0

# MC 26.x runs on JDK 25; give Gradle room and let it parallelize/cache.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true
