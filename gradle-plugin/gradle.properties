# This is an included build, so it does not inherit the root gradle.properties.
# The plugin ships on the same version line as the adapter and core-lib; keep
# this base semver in step with the root (drop -SNAPSHOT for a release).
group=dev.skein
version=0.1.0-SNAPSHOT
